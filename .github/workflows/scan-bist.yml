name: 📊 BIST Scanner

on:
  schedule:
    # ── Türkiye Saati: 10:30, 13:00, 15:30, 18:00 (Hafta içi) ──
    # ── UTC:           07:30, 10:00, 12:30, 15:00               ──
    # BIST açık: TR 10:00-18:00, günde 4 tarama
    # 30. dakikada başlat (Crypto ve Forex ile çakışmasın)
    - cron: '30 7 * * 1-5'
    - cron: '0 10 * * 1-5'
    - cron: '30 12 * * 1-5'
    - cron: '0 15 * * 1-5'
  workflow_dispatch:

concurrency:
  group: bist-scanner
  cancel-in-progress: true

jobs:
  scan:
    runs-on: ubuntu-latest
    timeout-minutes: 25

    permissions:
      contents: write

    steps:
      - name: 📥 Checkout Repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: 📦 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: 📚 Install Dependencies
        run: npm install

      - name: 🕐 Tarama Saati (TR)
        run: |
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "🇹🇷 TR Saati : $(TZ='Europe/Istanbul' date '+%Y-%m-%d %H:%M:%S')"
          echo "🌍 UTC Saati: $(date -u '+%Y-%m-%d %H:%M:%S')"
          echo "📊 Market   : BIST"
          echo "📊 Sembol   : 30 hisse (BIST-30)"
          echo "⏱️ TF       : M5 + M15 + H4 + D1 + W1"
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"

      - name: 📊 BIST Taraması Başlat
        timeout-minutes: 20
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: node src/scanner.js bist

      - name: 📤 Commit & Push Results
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"

          # Diğer workflow'lar da push yapmış olabilir, önce çek
          git pull --rebase origin main || true

          git add data/bist-signals.json

          if git diff --staged --quiet; then
            echo "📝 BIST: Değişiklik yok, commit atlanıyor"
          else
            TIMESTAMP=$(TZ='Europe/Istanbul' date '+%Y-%m-%d %H:%M')
            git commit -m "📊 BIST sinyal güncelleme: $TIMESTAMP"

            # Push başarısız olursa 3 kez dene
            for i in 1 2 3; do
              git push && break
              echo "⚠️ Push denemesi $i başarısız, tekrar çekiliyor..."
              sleep 5
              git pull --rebase origin main || true
            done

            echo "✅ BIST sinyalleri push edildi"
          fi
