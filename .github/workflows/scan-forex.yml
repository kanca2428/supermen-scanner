name: 💱 Forex Scanner

on:
  schedule:
    # ── Türkiye Saati: 10:00, 14:00, 18:00, 22:00 (Hafta içi) ──
    # ── UTC:           07:00, 11:00, 15:00, 19:00               ──
    # Günde 4 kez — Forex Pzt-Cum açık
    # 15. dakikada başlat (Crypto ile çakışmasın)
    - cron: '15 7 * * 1-5'
    - cron: '15 11 * * 1-5'
    - cron: '15 15 * * 1-5'
    - cron: '15 19 * * 1-5'
  workflow_dispatch:

concurrency:
  group: forex-scanner
  cancel-in-progress: true

jobs:
  scan:
    runs-on: ubuntu-latest
    timeout-minutes: 20

    permissions:
      contents: write

    steps:
      - name: 📥 Checkout Repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: 📦 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: 📚 Install Dependencies
        run: npm install

      - name: 🕐 Tarama Saati (TR)
        run: |
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "🇹🇷 TR Saati : $(TZ='Europe/Istanbul' date '+%Y-%m-%d %H:%M:%S')"
          echo "🌍 UTC Saati: $(date -u '+%Y-%m-%d %H:%M:%S')"
          echo "💱 Market   : FOREX"
          echo "📊 Sembol   : 20 parite"
          echo "⏱️ TF       : M5 + M15 + H4 + D1 + W1"
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"

      - name: 💱 Forex Taraması Başlat
        timeout-minutes: 15
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: node src/scanner.js forex

      - name: 📤 Commit & Push Results
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"

          # Diğer workflow'lar da push yapmış olabilir, önce çek
          git pull --rebase origin main || true

          git add data/forex-signals.json

          if git diff --staged --quiet; then
            echo "📝 Forex: Değişiklik yok, commit atlanıyor"
          else
            TIMESTAMP=$(TZ='Europe/Istanbul' date '+%Y-%m-%d %H:%M')
            git commit -m "💱 Forex sinyal güncelleme: $TIMESTAMP"

            # Push başarısız olursa 3 kez dene
            for i in 1 2 3; do
              git push && break
              echo "⚠️ Push denemesi $i başarısız, tekrar çekiliyor..."
              sleep 5
              git pull --rebase origin main || true
            done

            echo "✅ Forex sinyalleri push edildi"
          fi
