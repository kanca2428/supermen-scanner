name: 💱 Forex Scanner

on:
  schedule:
    # Türkiye saati: 10:00, 14:00, 18:00, 22:00 (Hafta içi)
    # UTC:           07:00, 11:00, 15:00, 19:00
    - cron: '15 7 * * 1-5'
    - cron: '15 11 * * 1-5'
    - cron: '15 15 * * 1-5'
    - cron: '15 19 * * 1-5'
  workflow_dispatch:

concurrency:
  group: forex-scanner
  cancel-in-progress: true

jobs:
  scan:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    permissions:
      contents: write

    steps:
      - name: 📥 Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: 📦 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: 📚 Install Dependencies
        run: npm install

      - name: 🕐 Tarama Saati
        run: |
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "🇹🇷 TR  : $(TZ='Europe/Istanbul' date '+%Y-%m-%d %H:%M:%S')"
          echo "🌍 UTC : $(date -u '+%Y-%m-%d %H:%M:%S')"
          echo "💱 20 Forex × 5 TF = 100 API çağrısı"
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"

      - name: 💱 Forex Tara
        timeout-minutes: 10
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: node src/scanner.js forex

      - name: 📤 Commit & Push
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git pull --rebase origin main || true
          git add data/*.json
          if git diff --staged --quiet; then
            echo "📝 Değişiklik yok"
          else
            TIMESTAMP=$(TZ='Europe/Istanbul' date '+%Y-%m-%d %H:%M')
            git commit -m "💱 Forex sinyal: $TIMESTAMP"
            git push
          fi
