name: 🪙 Crypto Scanner

on:
  schedule:
    # Türkiye saati: 01:00, 05:00, 09:00, 13:00, 17:00, 21:00
    # UTC:           22:00, 02:00, 06:00, 10:00, 14:00, 18:00
    # Günde 6 kez — Crypto 7/24 açık
    - cron: '0 22 * * *'
    - cron: '0 2 * * *'
    - cron: '0 6 * * *'
    - cron: '0 10 * * *'
    - cron: '0 14 * * *'
    - cron: '0 18 * * *'
  workflow_dispatch:

concurrency:
  group: crypto-scanner
  cancel-in-progress: true

jobs:
  scan:
    runs-on: ubuntu-latest
    timeout-minutes: 20

    permissions:
      contents: write

    steps:
      - name: 📥 Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: 📦 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: 📚 Install Dependencies
        run: npm install

      - name: 🕐 Tarama Saati
        run: |
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "🇹🇷 TR  : $(TZ='Europe/Istanbul' date '+%Y-%m-%d %H:%M:%S')"
          echo "🌍 UTC : $(date -u '+%Y-%m-%d %H:%M:%S')"
          echo "🪙 30 Crypto × 5 TF = 150 API çağrısı"
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"

      - name: 🪙 Crypto Tara
        timeout-minutes: 15
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          CRYPTOCOMPARE_API_KEY: ${{ secrets.CRYPTOCOMPARE_API_KEY }}
        run: node src/scanner.js crypto

      - name: 📤 Commit & Push
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git pull --rebase origin main || true
          git add data/*.json
          if git diff --staged --quiet; then
            echo "📝 Değişiklik yok"
          else
            TIMESTAMP=$(TZ='Europe/Istanbul' date '+%Y-%m-%d %H:%M')
            git commit -m "🪙 Crypto sinyal: $TIMESTAMP"
            git push
          fi
